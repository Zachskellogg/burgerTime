<h1>burgers!</h1>

<h2>burgers that are not eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that have yet to be in your tummy</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burgers-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a new burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Burgers:</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten<br>
    <input type="radio" name="eaten" value="0"> Uneaten
  </div>

  <button type="submit">Add Burger</button>
</form>
